# Where is .weights files?

Download and install [DarkNet](https://pjreddie.com/darknet/install/), after that .weigth files are in root directory. 
.cfg files and coco.names are also also there. See <code>ProcessorFactory.cpp</code> for details of using them.
